import re
from pathlib import Path
import pandas
import ..config

def extract_uid_and_nohash(wav_path):
    uid_pattern = r'^(\w+)_nohash_(\d).wav'
    label = train_wavs[0].parts[-2]
    x = re.match(uid_pattern, train_wavs[0].parts[-1])
    uid = x.group(1)
    nohash = x.group(2)
    return label, uid, nohash

def extract_meta_info(path):
    
    

if __init__ == '__main__':
    audio_path = 
